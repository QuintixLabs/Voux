services:
  voux:
    image: ghcr.io/quintixlabs/voux/voux:latest
    ports:
      - "8787:8787"
    environment:
      - ADMIN_USERNAME=admin
      - ADMIN_PASSWORD=change-this-password
    volumes:
      - ./data:/app/data
